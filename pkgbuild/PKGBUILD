# Maintainer: wahibre <wahibre at gmx dot com>
pkgname=qmtn-git
pkgver=0.5
pkgrel=1
pkgdesc="GUI for mtn (Movie Thumbnailer)"
arch=('x86_64')
url="https://gitlab.com/movie_thumbnailer/qmtn"
license=('GPL3')
depends=('qt6-base' 'qt6-svg')
optdepends=('qt6-imageformats: Preview webp images')
makedepends=('git')
provides=("${pkgname%-git}")
conflicts=("${pkgname%-git}")
source=('git+https://gitlab.com/movie_thumbnailer/qmtn.git')
md5sums=('SKIP')

pkgver() {
	cd "$srcdir/${pkgname%-git}"
	git describe --long --tags | sed 's/\([^-]*-g\)/r\1/;s/-/./g'
}

build() {
	cd "$srcdir/${pkgname%-git}"
	    qmake6 CONFIG+=use_git_version src
	make
}

package() {
	cd "$srcdir/${pkgname%-git}"
	make INSTALL_ROOT="${pkgdir}" install
}
